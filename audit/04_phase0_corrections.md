# Phase 0 Corrections

> Historical correction ledger. Current finding dispositions live in
> `audit/findings.json` and `audit/STATUS.md`.

## Correction Table

| Previous Claim | Why Wrong/Unsupported | Corrected Claim | Evidence | Affected IDs | Affected Files |
|---------------|----------------------|-----------------|----------|--------------|----------------|
| "open-set uncertainty hardcoded to 0.05" | Implied DINOv2 returns 0.05. Actual: DINOv2 returns 0.0 (fallback). Pipeline assigns 0.05 to MiewID/Landmark, 0.1 to other channels | Pipeline fabricates constant uncertainty values for non-DINOv2 channels (0.05 for MiewID/Landmark, 0.1 for others). DINOv2 appearance returns genuine 0.0 fallback when no evidential head is active. The aggregate is an uncalibrated mean of these constants. | enroll.py:52-57: `uncertainties[name] = 0.05` for MiewID/Landmark, `0.1` for generic. appearance.py:84: `epistemic, aleatoric = 0.0, torch.zeros_like(emb)` fallback. | CVI-P0-010 | findings.json, 03_findings.md, 01_truth_table.md |
| "trainer has no encode() method (P0-015)" | Did not read extract_embedding() at line 271 | `ArcFaceModel` already has `extract_embedding()` (line 271). Root defect: validation loop calls `model(images, labels)` in eval mode → returns embedding (not logits). CrossEntropy applied to embedding is invalid. Also: `loss_type` unused, MagFace config unused, label smoothing unused, early stopping unused, `use_amp`/`mixed_precision` duplication. | trainer.py:263-269: `forward` returns `emb` when `not self.training`. Line 525: `logits = model(images, labels)` in eval mode. Line 526: `F.cross_entropy(logits, labels)` applied to embedding. | CVI-P0-015 (original, now split) | findings.json, 03_findings.md |
| "MiewID-msv3: no license" | HF API 404 for one endpoint does not prove no license exists | **UNVERIFIED**: HF API returned 404 for LICENSE file, cardData license field not found. No determination can be made without inspecting upstream repo (github.com/WildMeOrg/wbia-plugin-miew-id), config files, and weight terms separately. | HF API: license file 404, card license not found. | CVI-P0-012 | findings.json, 03_findings.md, 07_licenses.md |
| "no ArcFace/MagFace weights exist anywhere" | Source-tree absence ≠ no external checkpoints | Default constructors for repository-defined heads initialize random parameters. Checkpoint-loading path exists (trainer.py exports/loads .pt). External checkpoints may exist but are UNVERIFIED. No validated checkpoint found for the canonical runtime path. | trainer.py constructor creates random ArcFaceHead (line 228: xavier_normal init). No checkpoint loaded in canonical CVI API path. | CVI-P0-004/5/6 (severity adjusted) | findings.json, 03_findings.md |
| "DINOv2 appearance: meaningful embeddings" | Finite deterministic normalized output ≠ meaningful ReID | DINOv2 appearance extraction is technically operational: loads, produces finite 384-d normalized output, deterministic. Canine identity discriminative performance is UNVERIFIED until corrected evaluator runs on leakage-safe protocols. | appearance.py verified. | (no ID needed) | STATUS.md, 02_architecture.md |
| "CVI API runs without error" | Constructor smoke test ≠ working identity system | CVI() constructor and a single enroll/search cycle execute without exception on the current machine. Enrollment/search correctness, open-set behavior, persistence, channel semantics, and video E2E remain unverified or known-broken. | api.py verified. | (no ID needed) | STATUS.md |
| "MiewID deterministic → functional" | Deterministic wrong preprocessing is still wrong | MiewID ONNX determinism confirmed. Input contract: FAIL (160 vs 440). Architecture parity: FAIL (AvgPool vs GeM). Identity performance: UNVERIFIED. | ONNX session inspection. | CVI-P0-001, CVI-P0-002 | findings.json, 03_findings.md |
| "SuperAnimal ONNX 9 KB proves dummy" | File size is corroboration; root evidence is code | `_HRNetWrapper.__init__` accepts `state_dict` but never applies it (line 109-112). A newly initialized 3-layer replacement is exported instead. The loaded checkpoint is discarded. | download_models.py:107-132 verified. | CVI-P0-007 | findings.json |
| "DNPMask creates new random UNet every call" | Misread constructor vs apply scope | One random UNet per `DNPMask` instance (created in `__init__`). Repeated `apply()` calls reuse the same untrained instance. | nose_print.py:116-118: `self._model = self._build_unet()` in __init__. apply() uses self._model. | CVI-P0-005 | findings.json |
| "15 P0 findings" | Inflated severity on several items | Reassessed: 8 P0, 8 P1, 6 P2, 2 P3 (see severity reassessment section) | (see below) | all | findings.json, STATUS.md |
| "WSL2 CUDA limitation" as error cause for GPU tests | Not proven by stack trace | `test_onnx_inference_benchmark` CUDA errors: subprocess calls `uv run --isolated --extra onnx-cpu` which references non-existent extra `onnx-cpu` → TEST_DEFECT. | pyproject.toml has `cpu` not `onnx-cpu`. | baseline | command_log.md |
